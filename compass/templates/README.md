# <Project>

<这段写目的：这个产品要持续解决什么。不要先写怎么装、怎么读文件。>

## Document map

功能 design 放在 `doc/`，每个大功能一份：

```text
doc/
└── <feature>_design.md
```

| 文档 | 是什么 |
|:-----|:-----|
| `doc/<feature>_design.md` | 该功能的 design |

有新的大功能就在这张表加一行，并补对应文件。不要把模块设计写回本 README。

如果项目需要跨 session 维护当前工作，可使用 `doc/todo.md`；完成的工作和永久 design 不留在 todo。

## License

<License>
