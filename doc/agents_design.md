# Agents

这是 `compass/AGENTS.md` 的 design。

安装时把这块规则 merge 进目标项目的根 `AGENTS.md`（Cursor / Codex / OpenCode）或根 `CLAUDE.md`（Claude Code）。Marker：`<!-- compass:start -->` / `<!-- compass:end -->`。marker 外的用户规则不动。

这块只约束怎么写文档、怎么实现、怎么测、怎么 review。产品本身怎么说明，看那个仓库的 README 和 `doc/`。

Merge 步骤见 [install_instruction.md](install_instruction.md) Step 3。
