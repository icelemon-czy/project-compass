# AI 提示词模板：每次对话开头加载

> 将以下内容作为每次 AI 对话的系统提示（或对话开头的第一条消息）。
> 根据实际内容替换 `{{...}}` 占位符。

---

## 推荐提示词

```markdown
# 项目上下文

## 代码地图（必读）
{{粘贴 L1-codebase-map/overview.md 的内容}}

## 全局规则（必读）
{{粘贴 L2-rules/global.md 的内容}}

## 当前会话状态（必读）
{{粘贴 L4-session/active-session.md 的内容}}

---

## 按需加载（根据当前任务选择）

### 当前计划
{{粘贴 L3-tasks/current-plan.md 的内容}}

### 模块特定规则
{{根据任务涉及模块，粘贴对应的 L2-rules/[module-name].md}}

---

## 工作指令

1. 你正在帮我继续上面"当前会话状态"中记录的工作
2. 请先确认你理解了当前进度和下一步
3. 每次对话结束前，帮我更新"当前会话状态"文档
4. 如果做了重要的架构决策，帮我记录到 decision-log.md
5. 遵守全局规则和模块特定规则中的所有约束
```

---

## Token 预算参考

| 文档 | 预估 Token | 备注 |
|------|-----------|------|
| overview.md | ~300-500 | 必加载 |
| global.md | ~400-600 | 必加载 |
| active-session.md | ~300-500 | 必加载 |
| current-plan.md | ~300-500 | 推荐加载 |
| module-map.md | ~400-600 | 跨模块时加载 |
| 模块规则 | ~300-400 each | 按需加载 |
| **总计（核心）** | **~1300-2100** | 约占 4k context 的一半 |

> 💡 核心上下文控制在 2000 token 以内，为实际代码和对话留足空间
