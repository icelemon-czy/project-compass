# 模块结构与依赖关系

> 详细的模块划分和依赖图。跨模块修改时加载此文件。

## 模块列表

<!-- 按你的项目实际层次分组（如按层/按领域/按服务），以下仅为格式参考 -->

### [分组名称 1]

| 模块 | 路径 | 职责 | 规模(约) |
|------|------|------|----------|
| [填写] | `[填写路径]` | [填写] | [填写] |
| [填写] | `[填写路径]` | [填写] | [填写] |

### [分组名称 2]

| 模块 | 路径 | 职责 | 规模(约) |
|------|------|------|----------|
| [填写] | `[填写路径]` | [填写] | [填写] |
| [填写] | `[填写路径]` | [填写] | [填写] |

## 依赖图

```
[用 ASCII 描述模块间的依赖方向]

例：
模块A ──→ 模块B ──→ 模块C ──→ (无外部依赖)
            │                      ↑
            └──→ 基础设施层 ────────┘

禁止方向：
  [填写，如 核心层 ✗──→ 基础设施层]
```

## 模块间通信方式

| 来源 → 目标 | 方式 |
|-------------|------|
| [填写] → [填写] | [填写，如 函数调用 / HTTP API / 消息队列 / 事件] |
| [填写] → [填写] | [填写] |

## 共享代码

<!-- 列出跨模块共享的代码位置 -->

- `[填写路径]` — [说明]
- `[填写路径]` — [说明]
