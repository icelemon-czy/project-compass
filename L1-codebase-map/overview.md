# 项目全局概览

> ⚠️ 这是 AI 每次对话**必读**的文件，请保持精简（建议 < 100 行）

## 项目基本信息

- **项目名**: [填写]
- **一句话描述**: [填写]
- **技术栈**: [填写，如 Python + Django + PostgreSQL / Java + Spring Boot + MySQL / …]
- **代码规模**: [填写，如 ~5 万行 / ~20 万行]
- **仓库结构**: [填写，如 monorepo / polyrepo / 单仓库]

## 高层架构

```
[用简单的 ASCII 图或描述，展示系统的核心层次]

例（Web 应用）：                 例（CLI 工具）：
┌──────────────┐              ┌──────────────┐
│  Presentation│              │     CLI      │
├──────────────┤              ├──────────────┤
│  Application │              │    Core      │
├──────────────┤              ├──────────────┤
│   Domain     │              │   I/O Layer  │
├──────────────┤              └──────────────┘
│   Infra      │
└──────────────┘
```

## 顶层目录结构

```
project-root/
├── [填写]/            ← [说明]
├── [填写]/            ← [说明]
├── [填写]/            ← [说明]
├── tests/             ← [测试，可选]
├── scripts/           ← [脚本，可选]
├── docs/              ← [文档，可选]
└── [配置文件]         ← [如 package.json / pyproject.toml / pom.xml …]
```

## 核心业务概念（领域术语表）

<!-- 列出项目中最重要的业务概念，帮助 AI 理解领域语言 -->

| 概念 | 说明 |
|------|------|
| [填写] | [填写] |
| [填写] | [填写] |
| [填写] | [填写] |

## 关键约束

<!-- 列出 AI 编码时必须遵守的硬性约束 -->

- [填写]
- [填写]
- [填写]

## 构建和运行

<!-- 写清楚具体命令，减少 AI 每次猜测/搜索的时间成本 -->

```bash
# 环境前置要求
[填写，如 Node.js >= 20 / Python >= 3.12 / Java 21 + Maven …]

# 安装依赖
[填写命令]

# 启动开发环境
[填写命令]

# 运行测试
[填写命令]

# 常见问题
# [填写，如 首次运行需要先执行 xxx]
```
