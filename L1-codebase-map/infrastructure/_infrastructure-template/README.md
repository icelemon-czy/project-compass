# 基础设施层

> 本文件夹文档化跨功能的底层架构和基础设施（项目级共享基础设施）。
> **加载时机**：当任务涉及框架基类、配置系统、插件机制、构建流程、测试基础设施等时加载。
>
> 📂 **本文件夹结构**（按实际项目调整）：
> - `_infrastructure-template/` — 内容格式参考（不要机械复制）
> - `[组件名]/` — 每个基础设施组件一个文件夹
>   - `README.md` — 组件概览 + 导航（**先读这个**）
>   - `[层名].md` — 按组件实际层次拆分，按需加载
>
> 💡 **Infrastructure 的范畴**：被 ≥2 个 feature 共享的底层机制，
> 如框架基类、配置系统、CI/CD 构建流程、测试框架/helpers、代码生成工具等。
> 只服务于单个 feature 的 helper 不放这里，放在对应 feature 文件夹下作为一个层文件。

## 组件索引

<!-- AI 根据任务类型选择性加载对应组件 -->

| 组件 | 加载时机 | 包含内容摘要 |
|------|---------|-------------|
| [如: framework/] | [如: 继承基类 / 用 DI 容器 / 改生命周期] | [如: 基类体系、DI 注册、生命周期钩子] |
| [如: config/] | [如: 加新配置项 / 改配置加载逻辑] | [如: 配置文件结构、加载顺序、环境变量映射] |
| [如: plugin-host/] | [如: 开发新插件 / 改插件加载 / 改扩展点] | [如: 插件接口、注册机制、生命周期] |
| [如: build-system/] | [如: 改 CI 流程 / 改构建脚本 / 加新构建步骤] | [如: 构建流水线、发布流程、环境配置] |
| [如: testing/] | [如: 改测试框架 / 加新 test helper / 改 mock 策略] | [如: 测试框架配置、通用 mock、fixture 管理] |

> 💡 **加载规则**：先读本 README（架构全景 + 变更影响）→ 根据上表按任务类型进入对应组件文件夹

## 架构全景

<!-- 写出基础设施各组件之间的关系，以及它们如何被上层功能使用 -->

```
[如:
  功能层（features/*）
    ↓ 使用
  框架基类（BaseController, BaseService）
    ↓ 依赖
  配置系统（ConfigLoader → 环境变量 + 配置文件）
    ↓ 被
  插件系统（PluginHost → 加载/卸载插件）
    ↓ 共用
  通用工具（Logger, ErrorHandler, Serializer）
]
```

## 变更影响

<!-- 改基础设施层的波及面通常很大 -->

| 当你改了… | 必须同步改… | 原因 |
|-----------|-------------|------|
| [如: BaseController 签名] | [如: 所有 extends BaseController 的 Controller] | [如: 签名变更导致编译错误] |
| [如: 配置 schema] | [如: .env.example + 所有读取该配置的模块] | [如: 运行时配置缺失导致启动失败] |
| [如: 插件接口] | [如: 所有 implements IPlugin 的插件] | [如: 接口变更导致插件不兼容] |
| [如: Logger API] | [如: 全局所有 Logger 调用点] | [如: API 变更导致日志丢失] |

## 使用者（哪些 Feature 依赖本组件）

<!-- 列出依赖本基础设施组件的功能，便于评估变更影响面 -->

| Feature | 使用的接口 | 说明 |
|---------|-----------|------|
| [如: user-auth] | [如: requireAuth() 中间件] | [如: 所有需要鉴权的端点] |
| [如: order-management] | [如: BaseService 基类] | [如: OrderService 继承 BaseService] |

## 已知陷阱

- ⚠️ [填写]
- ⚠️ [填写]

---

## 组件文件夹格式参考

> 以下是单个 `[组件名]/` 文件夹的内容结构参考。
> 组件名和数量由基础设施分析决定（如 framework/ / config/ / plugin-host/ / testing/）。
> 结构与 features 一致：文件夹 + README + 层文件。

### `[组件名]/README.md` 格式

```markdown
# [组件名]

> 本文件夹包含「[组件名]」基础设施组件的完整上下文。
> **加载时机**：[一句话说明什么任务时加载本文件]

## 分层导航

| 层文件 | 加载时机 | 包含内容摘要 |
|--------|---------|-------------|
| [如: loader.md] | [如: 改加载逻辑 / 加新配置源] | [如: 配置加载器、合并策略] |
| [如: schema.md] | [如: 加新配置项 / 改校验规则] | [如: Schema 定义、校验逻辑] |

> 💡 先读本 README → 根据上表按任务类型选择性加载层文件

## 关键文件

| 文件 | 职责 |
|------|------|
| [路径] | [一句话] |

## 核心机制

<!-- 该组件最重要的工作机制、初始化流程、扩展点等 -->

## 对外接口

<!-- 该组件暴露给上层功能使用的 API / 基类 / 工具函数 -->

| 接口 | 调用方 | 说明 |
|------|--------|------|
| [填写] | [填写] | [填写] |

## 变更影响

| 当你改了… | 必须同步改… | 原因 |
|-----------|-------------|------|
| [填写] | [填写] | [填写] |

## 已知陷阱

- ⚠️ [填写]
```

### `[组件名]/[层名].md` 格式

```markdown
# [层名]

> **加载时机**：[一句话说明什么任务时加载本文件]

## 关键文件

| 文件 | 职责 |
|------|------|
| [路径] | [一句话] |

## 核心机制

<!-- 该层最重要的工作机制 -->

## 对外接口

| 接口 | 调用方 | 说明 |
|------|--------|------|
| [填写] | [填写] | [填写] |

## 已知陷阱

- ⚠️ [填写]
```

---

> 💡 基础设施的改动影响面广，修改前务必先查上表的影响范围。
