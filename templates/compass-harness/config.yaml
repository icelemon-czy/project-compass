schema_version: 1

project:
  name: "{{PROJECT_NAME}}"
  summary: "{{PROJECT_SUMMARY}}"
  source_root: "{{SOURCE_ROOT}}"

commands:
  test: "{{TEST_COMMAND}}"
  build: "{{BUILD_COMMAND}}"
  lint: "{{LINT_COMMAND}}"

platforms:
  - codex
  - claude-code
  - opencode

generation:
  canonical_root: .compass-harness
  managed_notice: "Generated by Compass Harness. Edit .compass-harness sources instead."
