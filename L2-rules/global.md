# 全局规则

> 适用于整个项目的通用规则。每次对话必加载。
> 请根据你的项目实际情况填写 `[填写]` 部分。

## 技术栈

- **主语言**: [填写，如 TypeScript / Python / Java / Go / Rust …]
- **语言版本**: [填写，如 Python 3.12 / Java 21 / Go 1.22 …]
- **框架**: [填写，如 React + Express / Django / Spring Boot / Gin …]
- **数据库**: [填写，如 PostgreSQL / MySQL / MongoDB / Redis …]
- **包管理器**: [填写，如 pnpm / pip / maven / cargo …]
- **其他关键依赖**: [填写]

## 编码规范

### 命名约定

<!-- 按你的语言习惯填写，以下是需要约定的维度 -->

| 元素 | 约定 | 示例 |
|------|------|------|
| 文件名 | [填写，如 kebab-case / snake_case / PascalCase] | [填写] |
| 类/结构体 | [填写] | [填写] |
| 函数/方法 | [填写] | [填写] |
| 变量 | [填写] | [填写] |
| 常量 | [填写] | [填写] |
| 接口/协议 | [填写] | [填写] |

### 语言特定规则

<!-- 列出你的语言/框架中最重要的编码约定，例如： -->
<!-- TypeScript: strict mode、禁止 any -->
<!-- Python: type hints、docstring 格式 -->
<!-- Java: 用 Optional 不用 null -->
<!-- Go: error 必须处理 -->

- [填写]
- [填写]
- [填写]

### 导入 / 依赖管理

- 导入顺序：[填写，如 标准库 → 第三方 → 内部模块 → 相对路径]
- 路径别名：[填写，如 `@/` → `src/`，无则写"不使用"]
- 其他：[填写]

## 架构规则

- **架构模式**: [填写，如 Clean Architecture / MVC / 六边形 / 分层 / 微服务 …]
- **依赖方向**: [填写，如 外层→内层，禁止反向]
- **错误处理策略**: [填写，如 Result 模式 / 异常 + 全局捕获 / error code …]
- **数据验证策略**: [填写，如 边界层验证 / DTO 验证 / 中间件验证 …]
- **其他架构约束**: [填写]

## 版本控制规范

- **分支策略**: [填写，如 Git Flow / Trunk-based / GitHub Flow …]
- **分支命名**: [填写，如 `feat/xxx`, `fix/xxx`, `refactor/xxx`]
- **Commit 规范**: [填写，如 Conventional Commits / 自定义格式]
- **PR/MR 要求**: [填写，如 必须包含描述、测试说明、影响范围]

## 构建与验证

<!-- 参考 GitHub Copilot 官方建议：把构建/测试/验证命令写清楚，减少 AI 探索时间 -->

```bash
# 安装依赖
[填写命令]

# 构建项目
[填写命令]

# 运行测试
[填写命令]

# 代码检查 / Lint
[填写命令]

# 格式化
[填写命令]
```

## 测试规范

- **测试框架**: [填写，如 Jest / pytest / JUnit / Go test …]
- **测试文件位置**: [填写，如 同目录 `xxx.test.ts` / 单独 `tests/` 目录]
- **覆盖要求**: [填写，如 核心业务逻辑必须有单元测试]
- **测试命名**: [填写，如 `should_xxx_when_yyy` / `test_xxx`]
