# 模块规则模板

> **使用方法**: 复制本文件，重命名为模块名（如 `api.md`、`ui.md`、`data.md`），
> 然后根据实际模块情况填写内容。
>
> 仅在处理该模块相关任务时加载。

## 模块信息

- **模块名**: [填写]
- **路径**: [填写，如 `src/api/`、`lib/core/`]
- **职责**: [一句话描述该模块的核心职责]
- **维护者**: [填写，可选]

## 模块特定技术

<!-- 该模块使用的特定框架/库/工具 -->

- [填写，如 该模块使用 Express 处理 HTTP 请求]
- [填写，如 该模块使用 SQLAlchemy 访问数据库]
- [填写]

## 模块编码约定

<!-- 该模块内特有的编码约定，与全局规则不同或更具体的部分 -->

### 文件组织

```
[描述该模块内文件的组织方式]

例：
module-name/
├── handlers/        ← 请求处理
├── services/        ← 业务逻辑
├── models/          ← 数据模型
├── utils/           ← 模块内工具
└── __tests__/       ← 测试
```

### 关键模式与约定

<!-- 该模块内需要遵循的设计模式或约定 -->

- [填写，如 所有 handler 必须通过中间件统一鉴权]
- [填写，如 数据库操作必须使用事务]
- [填写，如 响应格式统一为 `{ data, error, meta }`]
- [填写]

### 错误处理

<!-- 该模块特有的错误处理方式 -->

- [填写，如 Controller 层统一捕获异常并转换为 HTTP 响应]
- [填写，如 内部错误不得暴露给外部调用方]
- [填写]

### 安全考虑

<!-- 该模块需要特别注意的安全事项 -->

- [填写，如 用户输入必须经过验证和清洗]
- [填写，如 敏感数据必须加密存储]
- [填写，如 日志中不得包含敏感信息]

## 模块边界

<!-- 该模块与其他模块的交互规则 -->

| 交互方向 | 允许/禁止 | 方式 |
|----------|----------|------|
| 本模块 → [模块A] | ✅ 允许 | [如 直接调用 / API 调用 / 事件] |
| 本模块 → [模块B] | ❌ 禁止 | [原因] |
| [模块C] → 本模块 | ✅ 允许 | [如 通过接口/协议] |

## 测试要求

<!-- 该模块的特定测试要求 -->

- [填写，如 每个 handler 需要集成测试]
- [填写，如 业务逻辑需要单元测试，mock 外部依赖]
- [填写]
